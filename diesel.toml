# For documentation on how to configure this file,
# see diesel.rs/guides/configuring-diesel-cli

[print_schema]
file = "database/src/schema.gen.rs"
import_types = [
    "diesel::sql_types::*",
    "common::AccessRequestStateMapping",
    "common::CloudflareRoleMapping"
]
